node_modules
dist
dist-ssr
*.local
.DS_Store
Thumbs.db

# env files
.env
.env.local
.env.*.local

# editor
.vscode/*
!.vscode/extensions.json
.idea

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local Claude Code settings (machine-specific paths/permissions)
.claude/settings.local.json

# one-off build-script diagnostic output (e.g. scripts/buildEconomy.mjs's
# debug/economy-component-breakdown.json) — regenerable, not consumed by
# the app, not meant to be a permanent repo artifact
debug/
# same, but servable to the running dev server (e.g.
# scripts/buildEconomyWeo.mjs's economyScoresWeo.json, fetched at runtime by
# hud/useEconomyScoresWeo.ts) — under public/ so Vite serves it, still
# gitignored since it's trial data, not meant to ship
public/debug/
